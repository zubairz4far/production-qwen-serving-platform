global:
  scrape_interval: 10s

scrape_configs:
  - job_name: qwen-gateway
    static_configs:
      - targets: ["gateway:8080"]

  - job_name: vllm
    metrics_path: /metrics
    static_configs:
      - targets: ["vllm:8000"]
